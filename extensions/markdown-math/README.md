# Markdown Math

**Notice:** This extension is bundled with Vector Code. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to Vector Code's built-in markdown preview and markdown cells in notebooks.
